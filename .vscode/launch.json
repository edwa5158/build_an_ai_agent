// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "Attach to Python (debugpy)",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/main.py",
        "args": ["what files are in the root?"],
        "env": {
            "LOG_LEVEL": "debug"
        },
        "justMyCode": true
    }
  ]
}
